# P4 — OOXML Emitter (Tier 1 + linear gradient)

**Read first, in order:** `agent-tasks/phase5/SHARED.md` → `plan.md` §2, §3.3, §5 → `agent-instructions/AGENTS.pptx.md` (mandatory for all OOXML work) → this file. Apply with no discussion.

## What
Implement the OOXML emitter: absolute draw tree → native PPTX shapes. Primitives: `text` (box + anchor + align + insets, runs with bold/italic/color/size), `rect` (per-corner radius via `round1Rect`/`round2SameRect` adj values), `line`/`connector` (straight), `ellipse`, `image` (F7 fit modes fill/crop/contain — reuse existing image-fit code), `group` (transform propagation, paint order = document order), `gradient` (linear only: multi-stop, angle, alpha via `a:lin`), shadow via native `a:effectLst/outerShdw`.

## Why
This is the delivery artifact — the actual .pptx. Native OOXML throughout; no preview tricks (load-bearing rule #4).

## Owns (strict — nothing else)
- `PptxEditor.Core/Generation/Emit/Ooxml/`
- `DocxEditor.Tests/Generation/Emit/Ooxml/`

## Depends on
- **P2 merged** (absolute draw tree). P1 transitively.

## Acceptance
- Every Tier-1 primitive + linear gradient emits valid OOXML; generated decks open without repair (verify via round-trip through DocumentFormat.OpenXml validation or existing test helpers).
- Per-corner radius adj-value mapping covered by unit tests (OOXML `round1Rect`/`round2SameRect` semantics per AGENTS.pptx.md).
- spcPct units, regex attribute reads, per-shape normAutofit only — AGENTS.pptx.md compliance is acceptance.
- Smoke decks (REMOVED, pres-pro) still green.
- `dotnet test` green. Report per SHARED.md contract.
