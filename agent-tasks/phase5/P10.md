# P10 — Archetype Slide Functions

**Read first, in order:** `agent-tasks/phase5/SHARED.md` → `plan.md` §4, §8 (Q5 — expose in JSON), §7.3 → this file. Apply with no discussion.

## What
Implement archetype slide functions as thin compositions of P6 components: `cover`, `section`, `kpi_row`, `two_col`, `table_slide`. Expose them in the JSON vocabulary as slide types (e.g. `{"type": "kpi_row", ...}`) per §8 Q5's leaning — the AI thinks in slide types.

## Why
Archetypes are the prompt-friendly authoring surface; the 6-slide demo deck (§7.3) is built from them.

## Owns (strict — nothing else)
- `PptxEditor.Core/Generation/Archetypes/`
- `DocxEditor.Tests/Generation/Archetypes/`
- If JSON surface registration requires touching P1's schema: produce `integration/archetypes-schema.patch` instead of editing.

## Depends on
- **P6 merged** (components). Emitters transitively.

## Acceptance
- All 5 archetypes compose exclusively from P6 components (no direct primitive/emitter calls — enforce by inspection).
- JSON slide types validate and generate end-to-end.
- The 6-slide demo deck from one prompt-quality JSON is generated and visually reviewed next to pres-pro (family resemblance check, §7.3) — include the output path in your report.
- `dotnet test` green. Report per SHARED.md contract.
